# NOTE: something in completions.zsh messes with this, so this needs to be run after it

# plugin for smarter history searches and stuff
source "$HOME"/.local/share/ohmyzsh/plugins/fzf/fzf.plugin.zsh
