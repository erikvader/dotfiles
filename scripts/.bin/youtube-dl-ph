#!/bin/sh

for lastarg in "$@"; do :; done

output=
case "$lastarg" in
    *pornhub.com*|*iwara.tv*)
        output='%(title)s-%(uploader)s-%(id)s.%(ext)s'
        ;;
esac

exec youtube-dl ${output:+ -o "$output"} "$@"
