#!/bin/sh

for lastarg in "$@"; do :; done

output=
case "$lastarg" in
    *pornhub.com*|*iwara.tv*|*youtube.com*)
        output='%(title)s-%(uploader)s-%(id)s.%(ext)s'
        ;;
esac

exec youtube-dl --no-playlist ${output:+ -o "$output"} "$@"
